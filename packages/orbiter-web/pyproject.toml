[project]
name = "orbiter-web"
version = "0.1.0"
description = "Orbiter Web — AI agent platform UI and API server"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "aiosqlite>=0.20",
    "bcrypt>=4.0",
    "cryptography>=42.0",
    "croniter>=2.0",
    "httpx>=0.27",
    "orbiter-core",
    "orbiter-models",
]

[tool.uv.sources]
orbiter-core = { workspace = true }
orbiter-models = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/orbiter_web"]
